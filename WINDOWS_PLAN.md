# Minecraft 服务器一键开服工具 - Windows 版开发规划

由于 Windows 用户群体通常更偏好图形化界面且不熟悉命令行，Windows 版本的开发重心将从“脚本化”转向“工具化”。

## 1. 核心形态：GUI 可视化界面
*   **开发技术选型**：建议使用 Python (PyQt6/Tkinter) 或 Go (Wails/Fyne)，甚至可以使用简单的 C# (.NET)，以确保单文件运行且界面现代。
*   **启动流程**：
    1.  检测系统 Java 环境（自动弹出 Java 下载页面或内置下载逻辑）。
    2.  检测 Homebrew (Windows 版可使用 Winget 或直接管理二进制文件)。

## 2. 核心功能模块

### A. 服务器管理器 (Server Manager)
*   **自动扫描**：在工具同一目录下自动搜索已存在的服务器文件夹（识别 `server.jar`）。
*   **一键管理**：在界面列表中直接点击即可选择进入特定服务器的配置。

### B. 高级配置编辑器 (Properties Editor)
*   **可视化开关**：将 `server.properties` 中晦涩的配置项转化为开关或滑块。
    *   例如：正版验证 (online-mode)、PVP 开关、最大玩家数、视距 (view-distance)。
*   **汉化说明**：每个选项附带中文解释，防止用户配置错误。

### C. 自动化增强
*   **模组预设包**：允许用户选择“基础优化包”、“社交包”、“指令增强包”，脚本自动从数据库拉取 ID。
*   **权限初始化**：安装 LuckPerms 等模组后，自动生成一套预设好的权限组文件，解决“非管理员不能用指令”的问题。

## 3. 技术挑战与策略
*   **API 同步**：继续复用 macOS 版本的 Modrinth API 逻辑。
*   **并发下载**：Windows 版可引入多线程下载，提升同步模组的速度。
*   **异常捕获**：针对 Windows 常见的端口占用、内存溢出提供明显的弹窗报错。

---

## 4. 下一步开发阶段
1.  **第一阶段**：将 macOS 的 Shell 逻辑完整迁移为 Python 脚本。
2.  **第二阶段**：为 Python 脚本包裹图形化界面 (GUI)。
3.  **第三阶段**：集成 `server.properties` 可视化读写功能。
